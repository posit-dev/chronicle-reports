% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{chronicle_raw_data}
\alias{chronicle_raw_data}
\title{Load raw Chronicle data (Advanced)}
\usage{
chronicle_raw_data(
  metric,
  base_path,
  frequency = c("daily", "hourly"),
  ymd = NULL,
  schema = NULL
)
}
\arguments{
\item{metric}{Name of the metric to retrieve (e.g., "connect_users")}

\item{base_path}{Base path to Chronicle data directory}

\item{frequency}{Frequency of data collection: "daily" (default) or "hourly"}

\item{ymd}{Optional list with year, month, day for specific date filtering}

\item{schema}{Optional Arrow schema for the dataset}
}
\value{
Arrow dataset object
}
\description{
Loads raw Chronicle metric data. \strong{Most users should use \code{\link[=chronicle_data]{chronicle_data()}}
instead}, which provides curated data that is faster and easier
to work with.
}
\details{
Use raw data only when you need:
\itemize{
\item Custom aggregations not available in curated data
\item Hourly granularity
\item Specific timestamp filtering
}
}
\examples{
# Load raw daily Connect users data with sample data
sample_path <- chronicle_sample_data()
data <- chronicle_raw_data("connect_users", sample_path, frequency = "daily")

# View the data
dplyr::collect(data)

# Filter to a specific date using ymd parameter
data <- chronicle_raw_data(
  "connect_users",
  sample_path,
  frequency = "daily",
  ymd = list(year = 2024, month = 1, day = 1)
)
dplyr::collect(data)

\dontrun{
# Load from production Chronicle data
data <- chronicle_raw_data("connect_users", "/var/lib/posit-chronicle/data")

# Load hourly data for a specific date
data <- chronicle_raw_data(
  "connect_users",
  "/var/lib/posit-chronicle/data",
  frequency = "hourly",
  ymd = list(year = 2024, month = 12, day = 10)
)
}
}
